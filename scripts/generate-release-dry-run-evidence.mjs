import { execFileSync } from 'node:child_process'

const safeExec = (command, args) => {
  try {
    return execFileSync(command, args, { encoding: 'utf8', stdio: ['ignore', 'pipe', 'ignore'] }).trim()
  } catch {
    return ''
  }
}

const commit = safeExec('git', ['rev-parse', '--short', 'HEAD']) || '<commit>'
const date = new Date().toISOString().slice(0, 10)

const evidence = `# Release Dry-Run Evidence

Generated by \`npm run release:dry-run:evidence\`.

This is a dry-run evidence document only. It does not create a public release, upload assets, tag a commit, or publish a package.

## Dry-Run Scope

- Date: ${date}
- Commit: ${commit}
- Reviewer: <reviewer>
- GitHub CI URL: <paste GitHub Actions run URL>

## Required Commands

- [ ] \`npm run verify:examples\`
  - Result: <pass/fail>
  - Notes: Confirms the demo-safe portable board, checked open/export/save walkthrough, and SVG/export proof stay valid.
- [ ] \`npm run verify:public\`
  - Result: <pass/fail>
  - Notes: Confirms public readiness docs, templates, AGPL posture, and release-readiness guardrails stay present.
- [ ] \`npm audit --omit=dev\`
  - Result: <pass/fail>
  - Notes: Record dependency audit result.
- [ ] \`npm run check\`
  - Result: <pass/fail>
  - Notes: Confirms lint, build, SVG proof, board round-trip proof, example proof, and public readiness proof.

## Demo-Safe Proof

- [ ] \`examples/portable-flow.mapsmith\` remains synthetic/demo-only.
- [ ] The checked open/export/save walkthrough is current.
- [ ] SVG/export proof contains only synthetic labels.
- [ ] Proof material contains no private paths, secrets, credentials, tokens, private URLs, or real customer diagrams.

## Release Posture

- [ ] No public release was created.
- [ ] No release assets were uploaded.
- [ ] No git tag was created.
- [ ] No package was published.
- [ ] \`package.json\` remains \`private: true\`.

## Follow-Up

- Blockers: <none recorded>
- Known limitations: <none recorded>
- Follow-up issues: <none recorded>
`

process.stdout.write(evidence)
